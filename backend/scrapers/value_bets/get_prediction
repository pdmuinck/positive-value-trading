#!/bin/bash

get_prediction() {
    local sportradar_id=$1
    event_id=$(grep "${sportradar_id}" events.csv | grep 'pinnacle' | cut -d ',' -f 1 | tr -d '"')
    ./betoffers/pinnacle "${sportradar_id}" "${event_id}"
}

get_prediction $1












